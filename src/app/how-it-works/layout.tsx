export const metadata = {
  title: "How it works — PetPaw",
  description: "Learn how PetPaw QR tags keep your pet identifiable and safe.",
};

export default function HowItWorksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
