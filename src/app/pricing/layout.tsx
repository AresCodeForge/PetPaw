export const metadata = {
  title: "Pricing — PetPaw",
  description: "PetPaw QR keyring and plan pricing (Free, Pro).",
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
